import classNames from 'classnames/bind';
import styles from './TextTitle.module.scss';

const cx = classNames.bind(styles);

const TextTitle = () => {
    return <div className={cx('title-container')}>cc</div>;
};

export default TextTitle;
